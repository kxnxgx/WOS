# WOS（在庫日数）アイテム集約ツール — 総合仕様・業務マニュアル

本ドキュメントは、「WOS アイテム集約ツール」の最新仕様、計算ロジック、データ連携、画面の見方、および運用手順をまとめた総合マニュアルです。

---

## 1. システム概要と目的

本ツールは、全国のリテール直営店舗の売上履歴・現在庫・発注数データを多角的に分析し、**店舗間の在庫偏りを平準化して全社売上の最大化と在庫消化を促進する「アイテム移動推奨リスト」を自動生成するシステム**です。

### 🌟 主な特徴
1. **WOS（在庫日数/週数）に基づく科学的な在庫平準化**: 直近4週間の販売スピード（週販）と手元在庫から、過不足のない適正移動数を算出。
2. **消化率による2層集約（優先/通常）**: 消化率 ≥ 80% の商品は完売を狙う「⭐ 優先集約」、それ以外は「📦 通常集約」に分割。
3. **次シーズン継続品の色分け**: 次期（例: 26FW）発注がある継続品を行全体で青色ハイライト（🔄 バッジ付き）。
4. **自社BULK（倉庫）在庫の可視化**: 出荷予定振分の自社フリー在庫を E列に表示し、在庫あり（>0）セルを黄色ハイライト（⚠️ 店舗間移動より倉庫出荷を優先検討可能）。
5. **わかりやすい 16列対称レイアウト**: `[出荷側情報] ➔ 【移動推奨数】 ➔ [受入側情報]` の直感的な比較配置。
6. **手元実在庫の上限ガード**: 実在庫を超える出荷指示やマイナスWOSの発生を完全遮断。
7. **受入店舗の優先順位ルール（1:TOKYO 〜 9:NARITA）**: 必要度が同点の場合に旗艦店・主要店へ優先配分。

---

## 2. 参照ファイル（入力データ）一覧

ツール実行時、フォルダ内の最新ファイルを**キーワードで自動検出**します（引数での手動指定も可能）。

| ファイル種別 | 自動検索キーワード | 対象列・主な参照データ | 役割 |
|:---|:---|:---|:---|
| **売上データ** | `営業日付別売上分析` (.csv) | 列[1]:日付, 列[2]:店舗, 列[3]:SKU, 列[4]:ブランド, 列[6]:商品名, 列[8]:カラー, 列[10]:売上数 | 過去4週平均週販および累計売上数の算出 |
| **在庫データ** | `在庫一覧` (.csv) | 列[1]:店舗, 列[2]:SKU, 列[7]:ブランド, 列[17]:現在数量（R列） | 各店舗の手元現在庫数の取得 |
| **発注数データ** | `FRV` かつ `発注数` (.xlsx) | 1列目: SKU, 12列目: 発注計（L列） | 消化率（累計売上 ÷ 発注数）の算出 |
| **次期発注数データ** | `FW` かつ `発注数` (.xlsx) | 1列目: SKU, 12列目: 発注計（L列） | 次シーズン継続品（🔄）の判定 |
| **出荷予定振分** | `出荷予定振分` (.csv) | 4行目以降, D列(列[3]): SKU, **H列(列[7]): 納品先BULK** | 自社リテール専用BULK（倉庫）フリー在庫数の取得 |

---

## 3. 計算ロジックと業務ルール

### 3.1 フィルタリング・除外ルール
- **ブランド制限**: `FJALLRAVEN` のみ対象。
- **KANKEN除外**: SKUが `23510` で始まる定番KANKENは集約対象外。
- **Tokyo限定品除外**: 商品名に `Tokyo` を含む限定品は対象外。
- **特定店舗「6142」の除外**: 全社累計売上（消化率）の計算には含め、店舗間移動・WOS計算からは完全除外。
- **在庫一覧の「バルク」除外**: 他社在庫を含むため店舗間移動・WOS計算から除外（自社BULKは出荷予定振分H列のみを参照）。

### 3.2 WOS（Weeks of Supply: 在庫週数）
$$\text{WOS (週数)} = \frac{\text{現在庫数量}}{\text{直近4週間の平均週販}}$$
※ 直近28日間の売上がない店舗は WOS = NaN（集約の基準WOSから除外）。

### 3.3 消化率と優先度判定
$$\text{全社消化率 (\%)} = \min\left(100.0, \frac{\text{全期間累計売上数}}{\text{発注総数}} \times 100\right)$$
- **⭐ 優先集約**: 消化率 ≥ 80%（完売狙い）
- **📦 通常集約**: 消化率 < 80% または発注データなし

### 3.4 移動推奨数の算出とマッチングループ
1. 各SKUの「全店平均WOS」を基準とし、
   - **出荷候補店舗 (shippers)**: $\text{WOS} > \text{全店平均WOS}$（余剰あり）
   - **受入候補店舗 (receivers)**: $\text{WOS} < \text{全店平均WOS}$（不足あり）
2. **余剰数・不足数の計算**:
   - $\text{余剰数 (surplus)} = \text{現在庫} - (\text{平均WOS} \times \text{週販})$
   - $\text{不足数 (deficit)} = (\text{平均WOS} \times \text{週販}) - \text{現在庫}$
3. **受入店舗のソート優先順位**:
   - **第1キー**: 不足数（`deficit`）の降順（必要度が大きい店舗を最優先）
   - **第2キー（同点時タイブレーカー）**: 店舗優先順位ランク（昇順）
     1. TOKYO / 2. ルクア大阪 / 3. 名古屋 / 4. TOKYO NODE / 5. 京王新宿 / 6. 大丸心斎橋 / 7. 玉川高島屋 / 8. SAPPORO HUTTE / 9. NARITA
4. **実在庫上限ガード**:
   - $\text{移動推奨数} = \min(\lceil\min(\text{surplus}, \text{deficit})\rceil, \text{出荷元の手元現在庫})$
   - 1点移動するごとに手元在庫と余剰から移動数を引き、手元在庫が 0 になった店舗からの出荷は即座に終了。

---

## 4. 出力レポートの構成

### 4.1 移動推奨リストの 16列レイアウト

| 列 | カラム名 | 属性 | 説明 | 表示・強調例 |
|:---:|:---|:---|:---|:---:|
| **A** | **優先度** | 商品情報 | 優先/通常 | ⭐ 優先 |
| **B** | **商品コード** | 商品情報 | SKU | 23510... |
| **C** | **商品名** | 商品情報 | 商品名（継続品バッジ付） | 🔄 Kanken Laptop |
| **D** | **カラー** | 商品情報 | カラー名 | Black |
| **E** | **BULK在庫** | **倉庫情報** | **自社リテールBULKフリー在庫** | **5** *(⚠️黄色ハイライト)* |
| **F** | **消化率(%)** | 商品情報 | 全社累計消化率 | 85.2% |
| **G** | **出荷店舗** | 出荷側 | 出荷元店舗名 | FJALLRAVEN by 3NITY TOKYO |
| **H** | **出荷元在庫** | 出荷側 | 移動前の手元在庫 | 3 |
| **I** | **出荷前WOS** | 出荷側 | 移動前の出荷元WOS | 12.0週 |
| **J** | **出荷後WOS** | 出荷側 | 移動後の予測WOS（在庫減） | 4.0週 |
| **K** | **移動推奨数** | **移動指示** | **出荷元から受入先への指示数量** | **2** |
| **L** | **受入店舗** | 受入側 | 移動先店舗名 | FJALLRAVEN by 3NITY 大丸心斎橋 |
| **M** | **受入先在庫** | 受入側 | 移動前の手元在庫 | 0 |
| **N** | **受入前WOS** | 受入側 | 移動前の受入先WOS | 0.0週 |
| **O** | **受入後WOS** | 受入側 | 移動後の予測WOS（在庫増） | 8.0週 |
| **P** | **理由** | 補足説明 | 移動理由の詳細解説テキスト | ... |

### 4.2 ハイライト色分けルール
- 🟦 **行全体の薄い青色（`#e6f2ff` / `E6F2FF`）**: 次期継続品（26FW発注あり）。商品名に `🔄 継続品` マーク。
- 🟨 **E列セルの薄い黄色/オレンジ（`#ffeaa7` / `FFEAA7`）**: BULK在庫 > 0。倉庫に在庫があるため店舗間移動より倉庫出荷を優先検討可能。

---

## 5. 実行方法・運用手順

### 5.1 日常の実行手順
1. フォルダ内に最新の売上CSV、在庫CSV、発注数xlsx、出荷予定振分CSVを配置します。
2. `run_wos.bat` をダブルクリック（またはターミナルで `python run.py` を実行）。
3. 処理完了後、生成された `WOS_Report.html` または `WOS_Report.xlsx` を開いて移動指示を確認します。

### 5.2 コマンドライン引数（上級者向け）
```bash
python run.py --sales "売上.csv" --stock "在庫.csv" --order "発注.xlsx" --next-order "次期発注.xlsx" --ship-alloc "出荷振分.csv" --threshold 80.0
```
